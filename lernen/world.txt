Ordner: world
Zweck:
- Hier liegt die Welt-/Karten-Logik (Hintergruende, Bodenlinie, Zeichnung).

Datei: world\__init__.py
- Gesamtzeilen: 0
- Zeilen-Erklaerung:

Datei: world\game_map.py
- Gesamtzeilen: 81
- Zeilen-Erklaerung:
  - Zeile 1: `import pygame`
    -> Importiert ein Modul, das in dieser Datei benoetigt wird.
  - Zeile 2: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 3: `from core import settings`
    -> Importiert gezielt Namen/Funktionen aus einem Modul.
  - Zeile 4: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 5: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 6: `class GameMap:`
    -> Definiert eine Klasse als Bauplan fuer ein Objekt.
  - Zeile 7: `    def __init__(self):`
    -> Definiert eine Funktion/Methode mit eigenem Verhalten.
  - Zeile 8: `        self.background = None`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 9: `        self.floor_y_value = settings.HEIGHT - settings.GROUND_HEIGHT`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 10: `        self.draw_ground_overlay = True`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 11: `        self.ground_rect = pygame.Rect(`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 12: `            0,`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 13: `            self.floor_y_value,`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 14: `            settings.WIDTH,`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 15: `            settings.GROUND_HEIGHT,`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 16: `        )`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 17: `        self.ground_color = (42, 72, 54)`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 18: `        self.hill_color = (26, 42, 35)`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 19: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 20: `    @property`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 21: `    def floor_y(self) -> int:`
    -> Definiert eine Funktion/Methode mit eigenem Verhalten.
  - Zeile 22: `        return self.floor_y_value`
    -> Gibt einen Wert an den Aufrufer zurueck.
  - Zeile 23: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 24: `    def set_level_visuals(`
    -> Definiert eine Funktion/Methode mit eigenem Verhalten.
  - Zeile 25: `        self,`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 26: `        map_file: str | None,`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 27: `        floor_y: int | None = None,`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 28: `        draw_ground_overlay: bool = True,`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 29: `    ):`
    -> Startet einen neuen Code-Block (z.B. Bedingung/Schleife/Funktion).
  - Zeile 30: `        if floor_y is None:`
    -> Startet eine Bedingung: Code laeuft nur, wenn die Bedingung wahr ist.
  - Zeile 31: `            floor_y = settings.HEIGHT - settings.GROUND_HEIGHT`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 32: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 33: `        self.floor_y_value = max(0, min(settings.HEIGHT, floor_y))`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 34: `        self.ground_rect.top = self.floor_y_value`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 35: `        self.ground_rect.height = max(0, settings.HEIGHT - self.floor_y_value)`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 36: `        self.draw_ground_overlay = draw_ground_overlay`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 37: `        self.background = self._load_background(map_file)`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 38: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 39: `    def _load_background(self, map_file: str | None) -> pygame.Surface | None:`
    -> Definiert eine Funktion/Methode mit eigenem Verhalten.
  - Zeile 40: `        if not map_file:`
    -> Startet eine Bedingung: Code laeuft nur, wenn die Bedingung wahr ist.
  - Zeile 41: `            return None`
    -> Gibt einen Wert an den Aufrufer zurueck.
  - Zeile 42: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 43: `        map_path = settings.ASSETS_DIR / "Map" / map_file`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 44: `        if not map_path.exists():`
    -> Startet eine Bedingung: Code laeuft nur, wenn die Bedingung wahr ist.
  - Zeile 45: `            return None`
    -> Gibt einen Wert an den Aufrufer zurueck.
  - Zeile 46: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 47: `        image = pygame.image.load(str(map_path)).convert()`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 48: `        return self._scale_to_cover(image, settings.WIDTH, settings.HEIGHT)`
    -> Gibt einen Wert an den Aufrufer zurueck.
  - Zeile 49: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 50: `    @staticmethod`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 51: `    def _scale_to_cover(image: pygame.Surface, target_w: int, target_h: int) -> pygame.Surface:`
    -> Definiert eine Funktion/Methode mit eigenem Verhalten.
  - Zeile 52: `        src_w, src_h = image.get_size()`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 53: `        scale = max(target_w / src_w, target_h / src_h)`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 54: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 55: `        scaled_w = int(src_w * scale)`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 56: `        scaled_h = int(src_h * scale)`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 57: `        scaled = pygame.transform.smoothscale(image, (scaled_w, scaled_h))`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 58: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 59: `        result = pygame.Surface((target_w, target_h))`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 60: `        offset_x = (target_w - scaled_w) // 2`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 61: `        offset_y = (target_h - scaled_h) // 2`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 62: `        result.blit(scaled, (offset_x, offset_y))`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 63: `        return result`
    -> Gibt einen Wert an den Aufrufer zurueck.
  - Zeile 64: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 65: `    def draw(self, surface: pygame.Surface):`
    -> Definiert eine Funktion/Methode mit eigenem Verhalten.
  - Zeile 66: `        if self.background is not None:`
    -> Startet eine Bedingung: Code laeuft nur, wenn die Bedingung wahr ist.
  - Zeile 67: `            surface.blit(self.background, (0, 0))`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 68: `        else:`
    -> Ausweichfall, wenn keine vorherige Bedingung gegriffen hat.
  - Zeile 69: `            surface.fill(settings.BG_COLOR)`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 70: ``
    -> Leerzeile fuer Struktur und bessere Lesbarkeit.
  - Zeile 71: `        if self.draw_ground_overlay:`
    -> Startet eine Bedingung: Code laeuft nur, wenn die Bedingung wahr ist.
  - Zeile 72: `            hill_points = [`
    -> Speichert einen Wert in einer Variable oder einem Objektfeld.
  - Zeile 73: `                (0, self.ground_rect.top),`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 74: `                (170, self.ground_rect.top - 80),`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 75: `                (360, self.ground_rect.top - 20),`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 76: `                (560, self.ground_rect.top - 90),`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 77: `                (760, self.ground_rect.top - 30),`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 78: `                (settings.WIDTH, self.ground_rect.top),`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 79: `            ]`
    -> Fuehrt den naechsten konkreten Logikschritt aus.
  - Zeile 80: `            pygame.draw.polygon(surface, self.hill_color, hill_points)`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
  - Zeile 81: `            pygame.draw.rect(surface, self.ground_color, self.ground_rect)`
    -> Fuehrt einen Funktionsaufruf oder eine Methode aus.
